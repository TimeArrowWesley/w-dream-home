# 2026-09-15 資料夾清理

依業主要求清理不必要及舊版檔案，並維持既有 GitHub 精簡發布。

- 刪除 461 個檔案，共 367,477,880 位元組（350.45 MiB）。清理前 724.29 MiB，清理後約 374 MiB；不含 Git 歷史資料，新增說明與檢查紀錄僅占少量空間。
- 移除已撤回生成圖、舊 33 張圖集及底圖、舊壓縮包、24 張修正前 AI 初稿、舊 AI 視角底圖、一次性暫存及模型快照。
- 櫃體壓縮包已逐檔核對與保留的 5 張原始 PNG 完全相同，僅刪除重複包。
- 保留全部四版現行模型、144 個最新圖集視角、82 張最終 AI 原圖、完整來源模型及提示、最新離線看圖包、8 份原始 PDF、家具資料與仍被網站引用的設計對照。
- 原始紅圈平面圖整理至 `交接參考截圖/原始紅圈平面圖_20260914.png`，內容不變。
- 刪除前逐檔核對位置及 SHA-256；刪除後核對其餘 1,115 個檔案內容均未變更。後續只更新交接、README 與發布清單。
- 詳細刪除路徑、檔案大小與雜湊保存在本機同目錄的 `清理明細.json`、`清理結果.json`，不發布到網站。

舊交接已整合成根目錄的 `專案交接.md`。檔案日期不作為唯一清理依據；仍使用的舊命名模型及歷史比較圖保留。

## 清理後檢查

- 網站發布清單 516 個檔案、593 個 HTML 引用全部完整；網站約 64.18 MiB，GitHub 當前檔案約 65.09 MiB。
- 四版介面、版本與區域切換、平面圖放大、步行控制及家具操作通過離線 DOM 檢查。
- 圖集 144 個視角、48 組版本／空間及 288 次大圖互動通過檢查。
- 246 個成品／模型／縮圖檔可完整解碼；144 張來源模型雜湊一致。
- 最新離線圖集包含 253 個檔案，約 36.16 MiB，壓縮內容完整性檢查通過。

本次沒有重新進行瀏覽器或 GPU 外觀測試；3D 模型程式與圖像內容未變更。GitHub 只更新維護說明與發布清單，清掉的過期檔案原本就未納入 GitHub。
