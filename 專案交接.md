# W 夢想之家｜最新專案交接

更新：2026-09-15。以目前專案根目錄為唯一編輯來源；舊的「公司繼續編輯」及「回家繼續編輯」交接已整合到本文件。

## 開啟與版本

直接開啟根目錄 `index.html`，預設全戶鳥瞰；版本選單保留以下五版。

| 版本 | 名稱 | 入口 |
| --- | --- | --- |
| V0 | 原始格局 | `提案/原始格局/index.html?layout=v0` |
| V1 | 圓弧中島酒吧＋玄關矮櫃 | `index.html?layout=v1` |
| V2 | 旋轉電視＋小中島 | `提案/旋轉電視與直線中島/index.html?layout=v2` |
| V3 | 旋轉電視＋大中島 | `提案/開放大中島/index.html?layout=v3` |
| V4 | 大中島（南牆固定電視） | `提案/南牆電視與開放中島/index.html?layout=v4` |

V2、V3、V4 會共用提案資料夾內的程式，不可依資料夾名稱或日期直接判定為舊版刪除。歷史修正報告的版本編號可能不同，以本表及 README 為準。

V0 依本機格局.pdf 還原。原始圖面、向量擷取與尺寸依據在 `提案/原始格局/`，模型檢查在其 `核對/`；貓房與L型桌為原圖特徵。V0與根目錄共用設備／材質程式，不可刪除。

## 圖集與資料

- `AI寫實視角.html`：五版共 180 個視角，每版 12 區、每區 3 個方向，可切 AI／模型及放大下載。
- `成品圖集/20260914暗色現代工業/`：本次 9/15 完成的最新版圖集。資料夾名稱保留製作開始日期，並非過期版本。
- 圖集內 `originals/` 是 100 張最終 AI PNG；`model/` 是 180 張來源模型 PNG；`prompts/` 與生成紀錄供後續修圖。這些是可繼續編輯的原始素材，應隨完整本機專案保存。
- 網頁使用 `images/`、`models/`、`thumbs/` 內去重後的 WebP。相同底圖跨版本共用圖片，共 100 組檔案；180 個視角並未減少。
- `成品圖集/W夢想之家_五版全屋AI圖集_精簡包.zip`：最新離線看圖包，容量以壓縮檔為準，只包含圖集；要繼續修改 3D，請攜帶完整專案。
- 原始 PDF、報價單、交接截圖及櫃體正面原圖只存本機。原始紅圈平面圖已整理到 `交接參考截圖/原始紅圈平面圖_20260914.png`。

AI 效果採黑灰白、暗色現代工業，使用玻璃、鐵件、藝術水泥漆及深色木材。AI 是材質與光影示意，格局與尺寸以實際模型及核定圖說為準。

## 家具與共用功能

家具資料主檔為 `家具清單.json`，離線頁面使用同步的 `furniture-data.js`。在家具頁按「儲存到專案」寫回；若只能匯出，請保存 JSON。直接編輯 JSON 後執行 `node tools/sync-furniture-seed.cjs`。瀏覽器暫存不等於已寫回或已上傳 GitHub。

五版保留步行、90 度開門、廚房電動玻璃門、設備及收納操作、窗簾、RGB、燈光情境與效能設定。360 環景功能已移除。省電畫質預設按需繪圖，靜止時停止連續渲染。

## 尚需深化的設計

最新喇叭方案在 `提案/20260914喇叭配置/`；2D 修訂 B 的候選配置尚未套入即時 3D，請勿宣稱模型已依新候選更新。具體淨距、聲學、結構、機電、門片行程及設備安裝仍依供應商與施工圖核定。各項歷史限制見 README 與對應設計資料。

## GitHub 與後續維護

網站：https://timearrowwesley.github.io/w-dream-home/

儲存庫：https://github.com/TimeArrowWesley/w-dream-home

`.github/publish-files.json` 控制發布清單。只上傳網站需要的程式、圖集、仍引用的設計資料與可重用工具；私人 PDF、原始 AI PNG、製作底圖及 ZIP 不上傳。GitHub 下載包可使用 3D 和圖集，但不包含上述本機原始素材。

新增需上傳的檔案後，更新發布清單並執行 `python tools/build-site.py --sync-ignore` 與 `python tools/build-site.py --check`。不要以整個資料夾無差別上傳。下一次修改請先看本文件、README 及家具主檔；保留五個現行版本及已確認功能。
