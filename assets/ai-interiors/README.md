# AI 寫實視角｜2026-09-10

目前使用 18 張內建 image_gen 生成的室內效果圖，四版各 12 區。所有正式圖片以 `-20260910.png` 結尾，由 `catalog.js` 綁定版本與區域。舊三張首批圖片已由本批取代。

- 四版玄關分開生成，保留高矮櫃／90cm 矮櫃及背景曲線中島／旋轉電視差異。
- 原版 V1／V2 客廳因前景櫃高不同分開生成，圓弧中島取景相同所以共用。
- V3／V4 客廳與中島取景內配置一致，共用旋轉電視正面與背面兩張；冰箱、直線中島、電視、書房玻璃位置依最新模型參考，不套用舊圖的錯誤展示牆。
- 其餘九區模型配置一致，四版共用各區自己的圖片。
- 圖片不隨燈光、窗簾與家具旋轉即時改變。AI 圖可呈現材質感，實際尺寸以 3D 模型與正式圖說為準。

`generation-20260910.json` 保存每張圖片的完整提示詞、原始生成輸出位置及參考圖路徑，使用 built-in image_gen，未使用 API／CLI。`references/20260910/` 是本次由最新版模型擷取的幾何與取景依據，視高 165cm；背景裝飾及材質細節為 AI 表現。

舊的 `prompts.json` 僅為首批三張圖的歷史生成紀錄，不是目前發布圖目錄。360 功能及對應圖片已依業主要求移除。
